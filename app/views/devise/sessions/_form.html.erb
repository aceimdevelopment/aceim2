<h3>Bienvenido al Administrador de los Cursos de FUNDEIM - EIM - UCV </h3>
<p>
  <strong style="font-size: 20px;line-height: 135%;color: #555555;font-weight: bold;">
    &nbsp;*&nbsp;BIENVENIDOS *&nbsp;WELCOME *&nbsp;BEM-VINDO *&nbsp;BIENVENU *&nbsp; WILLKOMMEN *&nbsp;BENVENUTO *
  </strong>
</p>
<p>Ingresa para verlo en acción</p>
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="form-group">
      <%= f.email_field :email, autofocus: true, autocomplete: "email", required: true, placeholder: "Correo Electrónico", class: 'form-control input-login'%>
    </div>
    <div class="form-group">
        <%= f.password_field :password, autocomplete: "current-password", required: true, placeholder: "Contraseña", class: 'form-control input-login' %>
    </div>

    <div>
      <input name="user[remember_me]" type="hidden" value="0"><input type="checkbox" value="1" name="user[remember_me]" id="user_remember_me">
      <label for="user_remember_me">Recuérdame</label>
    </div>

    <button type="submit" class="btn btn-primary block full-width m-b">Entra</button>
    <%= render "devise/shared/links" %>
    <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
      <p class="text-muted text-center"><small>¿No tienes una cuenta de usuario?</small></p>
      <%= link_to "Crea una cuenta", new_registration_path(resource_name), class: "btn btn-sm btn-light" %><br />
    <% end %>
<% end -%>

